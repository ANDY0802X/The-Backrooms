{
  "name": "the-backrooms-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "install:server": "npm --prefix server install",
    "postinstall": "npm --prefix server install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
